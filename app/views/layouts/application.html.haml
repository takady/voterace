!!!
%html
  %head
    %meta{:content => 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type'}/
    %meta{:name => 'viewport', :content => 'width=device-width, initial-scale=1'}/
    %title= @title.present? ? "#{@title} | VoteRace" : 'VoteRace'
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    = favicon_link_tag
    = analytics_init if GoogleAnalytics.valid_tracker?
  %body
    %nav.navbar.navbar-default.navbar-fixed-top
      .container
        .navbar-header
          .navbar-toggle
            - if sign_in?
              - if current_page?(mypage_path)
                = link_to 'Logout', logout_path
              - else
                = link_to image_tag(current_user.image_url, class: 'avatar'), mypage_path
                = link_to new_race_path, class: 'btn btn-primary new-race' do
                  = fa_icon :plus, text: 'New Race'
            - else
              %li
                = link_to '/auth/twitter' do
                  = fa_icon :twitter, text: 'Login with Twitter'
              %li
                = link_to '/auth/facebook' do
                  = fa_icon :facebook, text: 'Login with Facebook'
          = link_to image_tag('voterace.jpg'), root_path, class: 'navbar-brand'
        #navbar.collapse.navbar-collapse
          %ul.nav.navbar-nav.navbar-right
            - if sign_in?
              - if current_page?(mypage_path)
                %li= link_to 'Logout', logout_path
              - else
                %li= link_to image_tag(current_user.image_url, class: 'avatar'), mypage_path
                %li
                  = link_to new_race_path, class: 'btn btn-primary new-race' do
                    = fa_icon :plus, text: 'New Race'
            - else
              %li
                = link_to '/auth/twitter' do
                  = fa_icon :twitter, text: 'Login with Twitter'
              %li
                = link_to '/auth/facebook' do
                  = fa_icon :facebook, text: 'Login with Facebook'

    - if current_page?(root_path) && !sign_in?
      %header.jumbotron
        .container
          %h1.title VoteRace
          %p.copy Which one would you vote for ?

    .container.main-content
      .row
        .col-md-12
          - if flash[:notice]
            .alert.alert-success
              = flash[:notice]
      .row
        .col-md-12
          - if flash[:alert]
            .alert.alert-danger
              = flash[:alert]
      = yield
    %footer.container-fluid
      .container
        .row
          .col-md-12
            %p.copyright.text-center= "© #{Time.zone.now.year} VoteRace."
